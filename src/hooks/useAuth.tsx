// Deprecated duplicate file. Re-export from contexts implementation to avoid divergence.
export { AuthProvider, useAuth } from '@/contexts/AuthContext';
export type { User } from '@/services/authService';
